{
  "name": "musica-salvaje-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check:ui": "node --check public/app.js",
    "check": "npm run typecheck && npm run check:ui && npm test",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "agents": "0.22.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.1.2",
    "@cloudflare/workers-types": "5.20260829.1",
    "typescript": "7.0.2",
    "vitest": "4.1.0",
    "wrangler": "4.127.1",
    "@types/node": "26.4.0"
  }
}
